listen: localhost:8080
dbs:
  auth:
    type: sqlite
    connector: authuser
    dir: ./authdb
  
jellyauth:
  enabled: true
#  set_admin: admin:password

echo:
  enabled: true
  messages:
    - "%s"
    - You said '%s'
    - "MESSAGE RECEIVED. ECHO RESPONSE: %s"
    - Server received %s

hello:
  enabled: true
  base: /hello

logging:
  enabled: true
